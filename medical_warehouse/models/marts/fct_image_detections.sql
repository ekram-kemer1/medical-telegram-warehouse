

SELECT
    CAST(NULL AS BIGINT)       AS detection_id,
    CAST(NULL AS BIGINT)       AS message_id,
    CAST(NULL AS TEXT)         AS channel_key,
    CAST(NULL AS INT)          AS date_key,
    CAST(NULL AS TEXT)         AS detected_class,
    CAST(NULL AS FLOAT)        AS confidence_score,
    CAST(NULL AS TEXT)         AS image_category
WHERE 1=0